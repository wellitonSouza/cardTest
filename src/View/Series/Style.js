import { makeStyles } from '@material-ui/core/styles';


const Style = makeStyles((theme) => ({
    root: {
      flexGrow: 1,
      height: '90vh',
      width: '100%',
      padding: '30px 30px',
    },    
  }));

export default Style;